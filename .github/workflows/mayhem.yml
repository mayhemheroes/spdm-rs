# The Mayhem worker: build the commit image from mayhem/Dockerfile, push it to ghcr, then run
# Mayhem over each Mayhemfile. Runs on manual dispatch and is called by the sync cron
# (sync.yml) only when upstream actually moved — we fuzz on real upstream movement or an explicit
# dispatch, NOT on every commit to the mayhem branch.
# Duration defaults to 1200s (20 min); override per dispatch for a longer campaign, e.g.
#   gh workflow run mayhem.yml --ref mayhem -f duration=14400   # 4h (needs a plan that allows it)
name: Mayhem

on:
  workflow_dispatch:             # manual on-demand run (gh workflow run mayhem.yml --ref mayhem [-f duration=14400])
    inputs:
      duration:
        description: 'Fuzz seconds per target (default 1200 = 20 min; e.g. 14400 = 4h). Longer needs a plan that allows it.'
        type: string
        required: false
        default: '1200'
  workflow_call:                 # sync.yml calls this (only when upstream moved), passing the rebased SHA
    inputs:
      ref:
        description: commit/branch to build (cron passes the freshly-rebased mayhem SHA)
        type: string
        required: false
        default: ''
      duration:                  # reusable callers (cron) leave the 20-min default; can request a longer run
        description: fuzz seconds per target
        type: string
        required: false
        default: '1200'

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build:
    name: build commit image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write          # push the commit image to ghcr.io/<org>/<repo>
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0
          ref: ${{ inputs.ref || github.sha }}     # build the cron's rebased SHA when called

      - name: Resolve build SHA
        id: sha
        run: echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

      - name: Log in to ghcr.io
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Image metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: type=raw,value=commit-${{ steps.sha.outputs.sha }}   # tag the exact built commit

      - name: Build and push (mayhem/Dockerfile)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: mayhem/Dockerfile
          push: true
          build-args: |
            COMMIT=${{ steps.sha.outputs.sha }}
            REPO_URL=${{ github.server_url }}/${{ github.repository }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
    outputs:
      image: ${{ steps.meta.outputs.tags }}
      ref: ${{ steps.sha.outputs.sha }}

  mayhem:
    needs: build
    name: 'fuzz ${{ matrix.mayhemfile }}'
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 6
      fail-fast: false
      matrix:
        mayhemfile:                 # one entry per mayhem/Mayhemfile_*
          - mayhem/Mayhemfile_algorithm_req
          - mayhem/Mayhemfile_algorithm_rsp
          - mayhem/Mayhemfile_capability_req
          - mayhem/Mayhemfile_capability_rsp
          - mayhem/Mayhemfile_certificate_req
          - mayhem/Mayhemfile_certificate_rsp
          - mayhem/Mayhemfile_challenge_req
          - mayhem/Mayhemfile_challenge_rsp
          - mayhem/Mayhemfile_deliver_encapsulated_response_certificate_rsp
          - mayhem/Mayhemfile_deliver_encapsulated_response_digest_rsp
          - mayhem/Mayhemfile_deliver_encapsulated_response_rsp
          - mayhem/Mayhemfile_digest_req
          - mayhem/Mayhemfile_digest_rsp
          - mayhem/Mayhemfile_encapsulated_request_certificate_req
          - mayhem/Mayhemfile_encapsulated_request_digest_req
          - mayhem/Mayhemfile_encapsulated_request_req
          - mayhem/Mayhemfile_end_session_req
          - mayhem/Mayhemfile_end_session_rsp
          - mayhem/Mayhemfile_finish_req
          - mayhem/Mayhemfile_finish_rsp
          - mayhem/Mayhemfile_fuzz_x509v3
          - mayhem/Mayhemfile_get_encapsulated_request_rsp
          - mayhem/Mayhemfile_heartbeat_req
          - mayhem/Mayhemfile_heartbeat_rsp
          - mayhem/Mayhemfile_key_exchange_req
          - mayhem/Mayhemfile_keyexchange_rsp
          - mayhem/Mayhemfile_key_update_req
          - mayhem/Mayhemfile_key_update_rsp
          - mayhem/Mayhemfile_measurement_req
          - mayhem/Mayhemfile_measurement_rsp
          - mayhem/Mayhemfile_psk_exchange_req
          - mayhem/Mayhemfile_pskexchange_rsp
          - mayhem/Mayhemfile_psk_finish_req
          - mayhem/Mayhemfile_psk_finish_rsp
          - mayhem/Mayhemfile_vendor_req
          - mayhem/Mayhemfile_vendor_rsp
          - mayhem/Mayhemfile_version_req
          - mayhem/Mayhemfile_version_rsp
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ needs.build.outputs.ref }}
      - name: Start analysis for ${{ matrix.mayhemfile }}
        uses: ForAllSecure/mcode-action@v1
        with:
          mayhem-token: ${{ secrets.MAYHEM_TOKEN }}
          args: --image ${{ needs.build.outputs.image }} --file ${{ matrix.mayhemfile }} --duration ${{ inputs.duration || '1200' }}
